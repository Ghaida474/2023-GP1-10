from django.urls import path
from . import views

app_name = 'admin_account'

urlpatterns = [
    #  path('admin-home/', views.admin_home, name='admin_home'),
    # path('admin-home/addAdmin/', views.addAdmin, name='addAdmin'), 
    # path('admin-home/addSANDF/', views.addSANDF, name='addSANDF'), 
]